name: common-extraction
channels:
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - numpy==2.2.6
      - pytest==9.1.1
      - requests==2.34.2
      - apache-libcloud==3.9.1
      - sentence-transformers==5.4.1

# 可选 MDL 评估不属于当前两组最终结果的必要步骤。需要时另行安装：
# CMAKE_ARGS="-DGGML_CUDA=on" FORCE_CMAKE=1 \
#   pip install llama-cpp-python==0.3.35
