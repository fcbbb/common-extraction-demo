Refactor this cluster.

Input JSON:
{payload}

Return this JSON schema. Do not return complete rewritten member files. Each member must contain only a strict unified diff and the mapping between deleted implementation hunks and inserted common.py call hunks. Number each file's `@@ ... @@` hunks from top to bottom starting at 1; these are the hunk IDs used below, not source line numbers:
{
  "library": {
    "path": "common.py",
    "content": "..."
  },
  "members": {
    "file_000.py": {
      "diff": "",
      "edit_mapping": [
        {
          "edit_id": "edit_1",
          "helper": "helper_name",
          "removed_hunks": [1],
          "added_hunks": [2],
          "relationship": "what deleted implementation is replaced by the common.py call"
        }
      ],
      "call_mapping": []
    }
  },
  "rationale": "..."
}
