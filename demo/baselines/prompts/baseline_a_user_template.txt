Refactor this cluster.

Input JSON:
{payload}

Return this JSON schema. Do not return complete rewritten member files, line numbers, or diffs. For every member, return only an ordered list of exact local edit intents:
{
  "library": {
    "path": "common.py",
    "content": "..."
  },
  "members": {
    "file_000.py": {
      "edits": [
        {
          "original": "exact source fragment to replace",
          "replacement": "exact replacement fragment",
          "rationale": "what this local change does"
        }
      ]
    }
  },
  "rationale": "..."
}

The host program applies each original/replacement pair to the original source, preserving all text that is not covered by an edit. Use an empty edits array for an unchanged member. The host generates the final member files and audit diffs.
